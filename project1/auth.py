# Implements auth and token issuance

def authenticate(username, password):
  # authenticate user
  if valid: 
    return issue_token(username)
  else:
    return None

def issue_token(username):
  # issue JWT
